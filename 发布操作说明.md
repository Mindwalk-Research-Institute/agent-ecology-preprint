# 发布操作说明（给用户）

本目录 `协作/发布_github/` 是**发布包**：把它当成 GitHub 仓库的根推上去即可，
不需要动论文正稿所在的 `任务/T01_正文/`（生成器只读它，从不写它）。

下面 1–5 是**一次性**手工步骤（只有你能做：GitHub 网页上的操作与建仓权限），
6–7 是以后每次内容更新都要走的流程。

---

## 0. 这个包里有什么

```
README.md  LICENSE  发布操作说明.md  mkdocs.yml  requirements.txt
.github/workflows/pages.yml
paper/paper-cn-v5.pdf
docs/（首页 + 19 章 + 附录 A + 参考文献 + 两个静态资源目录）
```

`docs/` 与 `paper/` 都是**生成产物**：手改会在下次生成时被覆盖（见第 6 步）。

---

## 1. 在 GitHub 新建空仓库

- 建议仓库名：`agent-ecology-preprint`（或你自己的命名；仓库名会进站点 URL）。
- **不要**勾选 "Add a README file / .gitignore / license"——本包已自带 README 与 LICENSE，
  勾了会产生冲突提交。
- 可见性：先 Private 也行，Pages 打开后再切 Public。

## 2. 把本目录推上去

在本目录（`协作/发布_github/`）执行：

```bash
git init
git add -A
git commit -m "发布包：中文全稿 v5 + 阅读站（CC BY 4.0）"
git branch -M main
git remote add origin git@github.com:<你的用户>/<仓库名>.git     # 或 https 形式
git push -u origin main
```

> 想把它挂在已有仓库的子目录里也可以：把本目录整体复制过去，
> 然后在 `.github/workflows/pages.yml` 的 build 步骤里加
> `defaults: {run: {working-directory: <子目录名>}}`，
> 并把 `upload-pages-artifact` 的 `path` 改成 `<子目录名>/site`。

## 3. 开启 Pages（关键：Source 要选 GitHub Actions）

GitHub 网页 → 你的仓库 → **Settings** → 左侧 **Pages** → **Build and deployment**：

- **Source**：选 **GitHub Actions**（不是 "Deploy from a branch"）
- Branch：留空/无所谓（本包用 Actions 部署，不读分支）

本包的 `.github/workflows/pages.yml` 会在每次 push 到 `main` 时自动
`pip install -r requirements.txt` → `mkdocs build --strict` → 部署。
也可以在 **Actions** 页面点 "Deploy 阅读站到 GitHub Pages" → **Run workflow** 手动触发。

## 4. 取站点地址

push 后到 **Actions** 看那次运行变绿，再到 **Settings → Pages** 顶部即
`https://<你的用户>.github.io/<仓库名>/`（或 Environments → github-pages → 显示的 URL）。

## 5. 回填两处地址（一次性）

建仓后把地址写回引用格式与站点配置：

```bash
# 在发布包目录里执行；把 <用户>/<仓库> 换成实际值
SEDI='s|<仓库地址>|<用户>.github.io/<仓库>|g'
sed -i "$SEDI" README.md
# mkdocs.yml 末段与首段的两行注释掉占位，取消注释并填值：
#   repo_url: https://github.com/<用户>/<仓库>
#   site_url: https://<用户>.github.io/<仓库>/
git commit -am "回填仓库地址（README 引用格式与站点配置）" && git push
```

**自定义域名（可选）**：Settings → Pages → Custom domain 填域名（DNS 侧按 GitHub 要求
配 CNAME/CNAME 记录，仓库会生成 `CNAME` 文件或由 Pages 处理），然后把 `mkdocs.yml` 的
`site_url` 改成 `https://你的域名/`。子路径部署（`<用户>.github.io/<仓库>/`）时 `site_url`
要带 `/<仓库>/`，否则 Open Graph 与分享链接的绝对地址会偏。

---

## 6. 以后内容更新了怎么重生成

正稿在论文团队仓库（`任务/T01_正文/`）。**不要手改发布包里的 `docs/*.md`**，走生成器：

```bash
cd <论文团队仓库根>
# ① 正稿若改动过大，先跑一遍既有核对（bib 侧）
python 协作/工具/bib_precheck.py --head
# ② 重生成阅读站（章号重编、[cite:] 转换、脚注规范化、导航注入、PDF 副本）
python 协作/工具/发布站生成.py "$(pwd)" 协作/发布_github \
       协作/pdf/论文_中文_v5.bbl 任务/T01_正文/摘要_中文.md \
       协作/00_署名与单位.md 协作/pdf/论文_中文_v5.pdf
# ③ 本地严格构建，必须零告警
cd 协作/发布_github && python -m mkdocs build --strict
# ④ 推到发布仓库（若发布包就是本目录且已 remote 到 GitHub）
git add -A && git commit -m "重生成：<改了什么>" && git push
```

**PDF 若也重出**（正文/文献表变了就要重出，见论文团队 `协作/pdf/A9-1` 日志），
出片后要把新 PDF 同时放进两处再提交（生成器会自动做，但单独重出 PDF 时记得手工同步）：

```bash
cp 协作/pdf/论文_中文_v5.pdf 协作/发布_github/paper/paper-cn-v5.pdf
cp 协作/pdf/论文_中文_v5.pdf 协作/发布_github/docs/paper/paper-cn-v5.pdf
```

两份副本字节数应相同；README 里写的 MD5 也要跟着更新。

**新增/删除一章**时还要在 `mkdocs.yml` 的 `nav:` 里加/删一行——不加会被
`--strict` 直接拦下（这是故意的，防"站点静默少一章"）。

## 7. 每次部署后自查（两分钟）

- [ ] 首页题名、署名（应为 `vincent VE　漫步学派研究院`）、日期、摘要 399 字、关键词 7 项
- [ ] 侧栏 19 章 + 附录 A + 参考文献，点任一章：页首页尾都有上一章/下一章
- [ ] 任一页脚注能跳到页尾脚注区（第一章 10 条、附录 A 47 条）
- [ ] 正文任一「作者, 年」点击后跳到参考文献对应条（共 146 处链接）
- [ ] 首页 "下载 PDF" 能下到 92 页 PDF，字节数 811,480（MD5 见 README）
- [ ] 搜索框输入「剩余」「欲望—智能体回路」都有命中（中文检索配置没被删）

## 8. 常见问题

| 现象 | 原因与处理 |
|---|---|
| Actions 红：`not in nav` | 新增页面没进 `mkdocs.yml` 的 `nav:`，加一行即可 |
| Actions 红：`Unrecognized link` / `Anchor not found` | 页内相对链接或 `refs.md#key` 锚点缺失；生成器自带的"链接自检"也会打印同一处 |
| 公式显示成 `$...$` 原文 | `mkdocs.yml` 的 `markdown_extensions` 里 `pymdownx.arithmatex` 或 `extra_javascript` 的 MathJax 两行被删了 |
| 中文搜不到 | `plugins.search` 的 `separator`/`pipeline` 被改回默认（默认按英文分词，中文整段当一个词） |
| 脚注变成正文 | 脚注定义被拆成多行；生成器的 `join_footnotes` 会并成一行，别在 docs 里手改 |
| 站点没样式/字体卡住 | `theme.font: false` 被改成引用 Google Fonts，内网/墙内会白屏 |
| PDF 下载 404 | `docs/paper/` 副本丢了，或 `not_in_nav` 被删；按第 6 步补两份副本 |

## 9. 边界（谁负责什么）

- 排版投稿岗（本包作者）：生成与核对 PDF、生成站点、CC BY 4.0 LICENSE、本说明。
- **需要用户本人拍板的**：GitHub 账号与仓库名、Pages 开启、自定义域名、
  是否同时投 SSRN/arXiv、arXiv 主类（cs.AI / cs.HC / cs.MA）、许可证一致性、
  英文稿署名（英文品牌名在 `协作/00_署名与单位.md` 里标着「待用户确认」，
  确认前不要出现在任何对外页面/表单上）。
- 学术内容与文字（含 README 文案若你要改写措辞）属论文主笔与领队；
  本包里的摘要、关键词、题名、署名全部由生成器从正稿与署名文件取出，
  改这些要在**正稿/署名文件**里改后重生成，不要在 `docs/` 里改。
